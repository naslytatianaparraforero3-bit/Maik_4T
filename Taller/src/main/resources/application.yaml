server:
  port: 8080

spring:
  application:
    name: Taller

  datasource:
    url: jdbc:mysql://localhost:3306/gestion_tareas_db?serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true